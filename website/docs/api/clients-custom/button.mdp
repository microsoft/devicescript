## Server

-   [startButton](/api/servers/button) starts a button server on the current device.

```ts skip
import { gpio } from "@devicescript/core"
import { startButton } from "@devicescript/servers"

const gpbtn = startButton({
    pin: gpio(2),
})
```
