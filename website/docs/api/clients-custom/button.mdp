## Server

Use options to specify pins and mount a [button server](/api/servers/button) as well.

```ts skip
import { gpio, Button } from "@devicescript/core"

const gpbtn = new Button({
    pin: gpio(2),
})
```
