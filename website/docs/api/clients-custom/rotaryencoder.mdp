## Server

Use options to specify pins and mount a [rotary encoder server](/api/servers/button) as well.

```ts
import { gpio, Button } from "@devicescript/core"

const button = new RotaryEncoder({
    pin0: gpio(2),
    pin1: gpio(3),
})
```
