# Hex template literal

The `hex` template literal is a builtin template literal that can be used to create a `Buffer` from a hex string.
The buffer is stored in flash.

```js
const buf: Buffer = hex`00 ab 12 2f 00`
```
