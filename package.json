{
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "build": "cd compiler && yarn build",
        "build-fast": "cd compiler && yarn build-fast",
        "test": "node run -t jacs/run-tests/basic.ts && node run test"
    },
    "workspaces": [
        "compiler",
        "cli",
        "runtime/jacscript-vm"
    ]
}
