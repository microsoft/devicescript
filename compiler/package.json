{
  "name": "jacscript-compiler",
  "version": "0.0.0",
  "description": "Jacscript Compiler",
  "main": "./built/jacscript-compiler.node.cjs",
  "module": "./built/jacscript-compiler.js",
  "types": "./built/types/compiler/src/jacscript.d.ts",
  "scripts": {
    "dist": "yarn install && yarn build",
    "build": "node build.js",
    "build-fast": "node build.js --fast",
    "watch": "node build.js --watch"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.14.11",
    "typescript": "^4.5.4"
  },
  "dependencies": {
  }
}
